/*
Debemos lograr tomar un nombre con 'prompt' 
y luego mostrarlo por 'alert' al presionar el botón  'mostrar'*/
function mostrar()
{
	/*se puede escribir en una linea */
	//let mensaje = prompt ("ingrese nombre")

	let mensaje;
	mensaje = prompt ("Ingrese su nombre")
	/*undefined es el error que sale cuando la variable no a sido definida*/ 

	

	
	/*declaro la variable e inicializo con la cadena "ingrese nombre" */
	alert(mensaje);
}

